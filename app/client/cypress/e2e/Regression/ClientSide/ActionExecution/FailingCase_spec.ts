describe("clearStore Action test", () => {
  it("1. Feature 11639 : Clear all store value", function () {
    expect(true).to.be.equals(true);  // This will make the test case fail
  });
   it("2. Feature 11639 : Clear all store value", function () {
    expect(true).to.be.equals(false);  // This will make the test case fail
  });
});
